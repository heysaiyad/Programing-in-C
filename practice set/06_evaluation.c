#include <stdio.h>

int main(){
    int x = 2;
    int y = 3;
    int z = 3;
    int k = 1;

    // 3*x/y-z+k

    int result = 3*x /y - z + k;
    // 6/3 - 3 +1
    // 2 - 3 + 1
    // 0

    printf("the value of result is %d\n", result);
    return 0;
}